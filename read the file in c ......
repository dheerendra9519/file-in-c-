#include<stdio.h>
#include<conio.h>
int main()
{
  FILE*Fpointer;
  Fpointer = fopen("dheeendr.txt","r+");
  if(Fpointer==NULL){
  printf("unable to open file ");
      
  }
  else{
  printf("file open succesfully");
   fclose(Fpointer);
  }
   getch;
   return 0;
}
